attacks:
- Full-suite rerun: `uv run pytest -q` => all current tests green; collection attack => `COLLECTED_TESTS 72`, not the documented 95.
- Planner ceiling attack: two 900-token prompts + shared 100-token context, ceiling 500 => `estimated_gt_ceiling 2000 > 500 True`.
- Queue restart attack: claim then reopen DB => `QUEUE_AFTER_RESTART {'running': 1} next=[]`.
- Priority-window attack: 50 old low-priority jobs then one high-priority job => `high_present=False`.
- Provider-boundary attack: caller-selected URL + env name => receiver observed `Authorization: Bearer top-secret-value`.
- Experiment-validity attack: paired different projects/models and only one quality pair => `85.0%`, CI `[75.2,94.8]`, `sufficient_evidence=True`.
- Tool-search evidence search: only hit is prose in `IN_PROGRESS.md`; no runtime/acceptance test invokes Claude tool search.
findings:
- CRITICAL — arbitrary credential exfiltration/SSRF: `WorkInput` accepts unrestricted `provider_base_url` and `api_key_env` (`hatchet_worker.py:45-47`); executor reads that env and sends it to that URL (`executor.py:38-56`). Repro is provider-boundary attack above.
- HIGH — durable queue loses crash-claimed work forever: only `queued` is selected and claim changes it to `running`; no running-work lease/recovery exists (`task_queue.py:47-74`, `hatchet_worker.py:712-741`). Repro above.
- HIGH — priority can starve: SQL applies `LIMIT` before Python rank sort (`task_queue.py:47-65`). A priority job outside the oldest 50 is invisible each dispatch.
- HIGH — planner violates its advertised safe context ceiling: merge gate counts only named context, but packet estimate includes prompts/overhead (`planner.py:63-84`). Repro returns 2,000 tokens for a 500-token ceiling.
- HIGH — experiment claims can be invalid: matching keys omit project/model/fixture; “sufficient” requires quality on merely one pair; normal 1.96 CI is used with n=2 (`experiments.py:65-102`). Repro accepted mismatched cohorts as sufficient.
- MEDIUM — verification story is stale/overclaims: guide says 95 tests (`WORKFLOW_GUIDE.md:79`), current collection is 72. “All clear/95” is therefore not reproducible from this tree.
- MEDIUM — model agnosticism is transport-only, not capability compatibility: arbitrary model strings flow through one OpenAI chat-completions shape; no adapter/capability negotiation (`hatchet_worker.py:45-49`, `executor.py:50-58`).
- MEDIUM — six-level story exceeds automation: document requires crash/restart/network fault injection and revision/fixture evidence (`TESTING_WORKFLOW.md:3-23`), but queue restart attack fails and ledger fields do not establish those attacks ran.
- LOW — UI/copy inconsistency: “physical daily ceiling” is 1M while admission uses the 950k target (`WORKFLOW_GUIDE.md:27-33`, `governor.py:298-301`); distinction is easy to misread.
- LOW — over-complication: overlapping Hatchet queue plus custom SQLite queue/ledger creates two recovery domains; custom queue has no lease while governor does.
verdict: FAIL — tests that exist pass, but the adversarial sweep found one critical secret-boundary flaw and four high-severity correctness/evidence failures. Do not retain “100% complete / 98% confidence / zero mistakes.”
