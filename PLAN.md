- ✅ ActivityWatch install

## Research-paper site (2026-08-19)
- `research-paper/index.html` served on localhost:3000 (start: `python3 -m http.server 3000 --directory research-paper`). WHY: his ask — an in-depth-but-simple evidence page comparing workflow vs no-workflow, incl. fan-out vs one-AI raw numbers. All figures pulled from langgraph-shadow/EVIDENCE.md (CI-locked); tags PROVEN/MEASURED/NOT-YET — no overclaim (live guard saving stays 0–10.81% until canary).

## Public.com arbitrage research (2026-08-22)
- `public-arb/RESEARCH.md` — feasibility study of Public.com's agentic brokerage ("Publix" = voice-dictation error for Public.com). WHY: his ask — is arbitrage possible, stocks + crypto, LIT vs smart vs wholesale routing, changelog in depth, and can AI agents reach near-Wall-Street level.
- **Decided: do NOT build a trading system yet.** Verdict is that true/latency arbitrage is structurally impossible on Public (REST-only, 10 req/s, no streaming feed), cross-venue crypto arb is dead on fees (20–120 bps round trip vs <10 bps spreads), and options-rebate harvesting is contractually prohibited with discretionary clawback (rebate T&C §2(e)/(f)).
- **Decided: gate any build behind two zero-capital falsification tests first** — (1) two weeks of SPX box implied-rate polling vs the 3.3% cash sweep, (2) a full bond-universe dump through the 2026-08-05 search endpoint to count >50bps issuer-curve outliers. WHY: three of the four surviving edges may not exist at current rates; find out with read-only calls before writing any framework.
- **Decided (architecture, if it ever gets built): Public API is an OMS only, never the market-data source**, and the LLM stays out of the order path — it belongs in bond-document comprehension, corporate-action monitoring, strategy authoring, and anomaly diagnosis. WHY: pricing is deterministic math, and Public's REST quotes are too stale/rate-limited to be a signal feed.
- **Not built, deliberately:** Public's own no-code AI Agents as the execution layer (human pre-approval per workflow, undisclosed evaluation cadence, Public-only data). Hard ceiling regardless: the Individual API is "personal, non-commercial use," so this can never be a product for others.

## Public.com arb — routing + Agents follow-up (2026-08-22)
- **Found: route selection is NOT in the API.** Place-order body has 13 fields, none of them route/venue; changelog never mentions routing. Route is a UI-only per-order control (fee schedule eff. 2026-07-16: wholesale $0, Smart $0.003/sh, **Lit Exchanges Only $0.003/sh — now live**, excluded for OTC/Investment Plan/Direct Index/Generated Assets). WHY it matters: §5.3 overnight convergence is all passive resting limit orders, and a limit order at a wholesaler is not displayed.
- **Decided: workaround is multi-account, not per-order.** One brokerage account defaulted to wholesale (taking), one to LIT (providing); the agent picks the account. Also doubles the 10 req/s budget. `auto-set — confirm at test time`: unverified that route default is per-account and persists for API orders.
- **Decided: gate §5.3 behind two written questions to Public support** (which route do API orders take; can defaults differ per account) — now step 0 of the sequence, ahead of the box-rate test.
- **Decided: Public's own AI Agents are the RISK layer only, never execution.** Best documented cadence is every 5 min vs the API's 10 req/s (~3,000×). Knobs are capital cap / per-asset stop / cadence / max trades-per-day — no preflight, no PUT-replace chasing, no route, no tax lots. Their one real edge: run inside the broker with no API keys and no machine of yours online, which suits the §5.4 tax-lot and cash-sweep jobs.
- **Corrected (2026-08-22, second pass): Public's native Agents are a real rules engine, not "retail automation."** Shipped 2026-03-31, waitlist rollout. EMA/SMA/RSI/MACD/Bollinger/ATR, market+limit, multi-leg options, shorting, cash movement, and event-chained sequences (assignment → next covered call). My earlier "5-minute cadence ceiling" was wrong — that was a user-set throttle example; Public says agents monitor continuously.
- **Decided anyway: Agents never carry an edge strategy.** WHY: Agentic Brokerage Agreement §B.5 disclaims liability for "any Transaction that is triggered, fails to be triggered, or improperly executed" on third-party data, and §B.4 gives no execution guarantee — a thin-margin edge is exactly the case where a missed trigger is the whole P&L. Plus no routing control, no self-set account caps (Webull has them), no paper trading, unattended with no per-trade alert.
- **Decided: Agents DO own the non-edge jobs** — cash sweep to the 3.3% account, covered-call writing/rolling/assignment handling, portfolio stops. WHY: runs inside the broker with no API keys and no machine of mine online, and their options-lifecycle logic is better than what I'd write.
- **Revisit trigger:** bonds and tax lots are "coming soon" as agent capabilities. When tax lots land, §5.4 harvesting may move from API to Agents — the only §5 job whose risk profile fits.
- **Asked (2026-08-22): build a 30%/yr system from Agents + all new features. Answer: it does not exist, and §9 shows the work.** Catalogued every unexamined feature — Generated Assets (0.49%/yr, agent swarm + backtest), Direct Index (0.19%, $1k min), Bond Account (~5–5.5% YTW), Jiko T-bills, Fractional Bonds, Premium. WHY no: 30% needs 26.7% over the 3.3% risk-free, and the only four sources (beta×leverage capped at Reg-T 2x, the volatility risk premium at 6–10%, genuine alpha, capacity-constrained inefficiency) each fail on arithmetic, not on effort.
- **Found: margin is 4.90% base → ~3.95% tiered** (was flagged unknown). **Kills the bond carry trade** — borrow 4.9%, hold 5.0–5.5% medium-credit corporates, minus 10–50 bps markup ≈ zero net for credit + duration risk on leverage.
- **Downgraded §5.2: do NOT run bond RV on Public's Fractional Bonds.** Single-dealer (Moment Markets quotes both sides), non-transferable, undisclosed spread on top of a $0.10–0.50/$100 fee. Can't arb the dealer who sets the price on an instrument only they will buy back. Strategy must use whole bonds.
- **Judged Generated Assets negative-edge:** prompt-until-the-backtest-looks-good is in-sample selection; Public's own disclosure names "Over-Reliance Risk." Expected alpha ~0 minus a certain 0.49%.
- **Decided: the honest ceiling is 9–14%/yr with a −25/−35% tail** (risk-free floor + leakage recovery + CSP wheel run by Agents, whose native template library IS the wheel lifecycle), or **4–5%/yr near-risk-free** if the wheel is dropped. Recorded as §9.3 with the stack diagram.
- **Decided: agent capacity goes to research, not execution** — whole-bond issuer curves, corporate actions, box-rate scanning. WHY: reading beats speed, and speed is the one thing this platform cannot give.

## Fleet landing — design pipeline S0–S9 (2026-08-22)
- **Decided: lamplight's identity lives in the treatment, not the prompt.** One press for every image — Bayer 8×8 halftone, scanline 4/0.22, `--ink #fba503 --paper #050506 --gamma 1.55 --floor 6 --ceil 238`. WHY: round 2 failed on three media (photo/painting/flat vector) sharing one surface; one press makes them one material. Gamma 0.85 and floor 34 were tried and rejected (killed the night / dot lattice under type).
- **Decided: palette is extracted, never invented** — `design-palette.py` over the six harbour raws gives paper `#050506`, ink `#bf651d` (7.16:1), lamp `#fba503`; page `:root` must equal the extraction (S7 gate enforces it). The original page shipped `#05060c` and failed the gate; plates re-treated to match the extraction rather than the other way round.
- **Fixed a prover bug (workflow-os `8611313`): the substrate shot now freezes transitions/animations.** Any element with `transition: color` rendered its glyphs mid-fade (85% opaque) after the text-hiding style tag, contaminating backdrop sampling — this was the "glyph/edge contamination" audit the a5dc7d0e job died inside. Two more real fixes it surfaced: the chip's own 1px border at ink-45% was ≥5% of its box and became the sampled backdrop (border dropped to the 30% hairline weight); the nav CTA needed a solid paper fill because the plate is hot behind the nav's right edge.
- **Pipeline weakness, still open: the S4t gate is size-only** — it passes untreated plates too; it cannot tell treated from raw. Belongs in the S4t gate as a palette-census check (a treated plate contains only ink/paper by construction).
- **S8 verdict: PASS from a fresh Sonnet critic on screenshots alone.** Two notes accepted as deliberate trade-offs, not fixed: the global halftone texture runs behind body copy (it is the anti-"world dies mid-page" move the gold-standard teardowns all failed); the hero holds three asks (primary CTA, honesty CTA, price line) because honesty is this page's actual pitch.
- **S9 picker written (`design/picker.html`, file://, neutral chrome so it doesn't pre-sell a candidate):** lamplight (built, gated) vs dreamscape (built, round-2 pick) vs three briefed worlds with locked graphs. **S10 blocked on his pick — by design.**

## Fleet landing — his round-3 feedback applied (2026-08-22)
- **Fixed the "broken" roll button mechanic and made it the button-effect family** — container `height:1.3em; overflow:hidden`, label duplicated into an absolutely-positioned `::after` at `top:100%`, hover `translateY(-1.3em)`. The break was the `::after` flowing in-layout, making the span 2.6em tall and spilling out of the button. Each project gets its own variant: dreamscape = plain roll, Flintted = roll + bottom fill-wipe ("clock-in"), lamplight = roll behind ledger hairlines.
- **Decided: one medium per page (his call, twice).** lamplight's band-2 painted ship swapped for `harbour-02--treated.png` — every lamplight plate is now a treated photograph; the painting lives only in dreamscape. WHY: "the ship … is cartoony … the lamp image looks out of place."
- **Motes are stratified, not random** — each mote owns `(i+0.5)/n` of the width plus a hashed ±0.45-slot jitter (hash `((i*2654435761)%1000)/1000`, no `Math.random`). WHY: his "less on the left and more on the right" was real clustering from uniform random draw.
- **Scheduler seam is a two-stop long fade** (bg 78% @14% → 34% @30% → transparent 46%) instead of a 22% cliff — his "top of the scheduler section is not blending in".
- **Scroll grammar shipped on both dreamscape pages**: `@supports (animation-timeline: view())` — plates pan/settle, cards settle 22px — plus IO reveal fallback with a 2.5s failsafe; compositor-only. Recorded as a universal pipeline rule in workflow-os `design/`.
- **Gates now PASS 390/768/1440 on fleet-dreamscape AND lamplight** after the honest-measurer reckoning: ink-mute 0.605→0.700, ink-dim→0.780, `--accent-text 0.855` for all small coral, h1 .soft .48→.62, deeper hero/#layers/.cta scrims, card fill gets a night-indigo base under the veil, deck cards restructured to claim-`p` + fine-print-`small` (23ch prose in a 214px column is a card, not a paragraph), figure column 300→344px, tag padding on the 4px grid, footer links 31px tall.
- **Fixed another prover lie (workflow-os): substrate sampling now insets 3px past the element's own border.** A 1px chip/tag outline was ~10% of a small box's pixels and got voted in as the 95th-percentile backdrop — all three lamplight "failures" were its own border, which no glyph ever touches.

## Fleet landing — S7 re-green, S8 scored vs gold, WORKFLOW.md (2026-08-22, resumed job)
- **Decided: one border system page-wide — evidence tags dropped from 55% to the chip's 32% hairline.** The prover (correctly, per its own rules) still sampled the tags' 1px 55% ink/lamp outline as the text backdrop and failed 2.85–3.65:1; rather than fighting the sampler, the tags join the hairline weight the chip already established. WHY: consistent instrument-panel line weight is the theme's own vocabulary; a scrim would be a generic overlay.
- **Decided: `.btn-ghost` gets the same solid paper fill as `.navcta`.** At 390px the wrapped CTA row lands on the plate's hot water and the transparent ghost button read 4.38:1 (flaky, sub-threshold). Same diagnosis, same established fix. S7 now PASS at 390/768/1440, run twice.
- **Decided: third plate band (`harbour-05--treated`, the seawall lamp) before #honest.** The scored critic named the text-only lower half — the exact "world dies mid-page" failure every gold page shares. The seawall lamp over dark water is the Honesty section's argument as an image: starvation happens under the light, never silently.
- **Found & fixed: `loading="lazy"` plates vanish from the S8 full capture.** design-fullshot.mjs never scrolls (CDP captureBeyondViewport), so the new band rendered 0px tall in the full shot (doc 6239 vs 6868). Lazy dropped on all three bands — 1-bit plates are 26–40KB, lazy bought nothing and cost the capture (and the reduced-motion reading of the page).
- **S8 official: SHOT-CRITIC PASS, three fresh-Sonnet runs** (before and after the fixes). Craft notes acted on: "month." orphan on phone (no-break join), price pill off the button baseline (`align-items:baseline`).
- **S8 scored, same-scale gold comparison: ours 38–40/70 (runs 40/38/39) vs Linear 36, Stripe 32, Cursor 28, Inkbox 21** — identical rubric/model/prompt on the archived gold shots. The prior 49/48/43/37 numbers were a kinder scale; on one scale ours tops the field, and still ties-or-beats the leader with the phone item excluded (golds have no phone shots). WHY re-scored instead of reused: cross-scale comparison is not evidence.
- **Palette re-verified from source**: `design-palette.py` over the six untreated harbour raws returns exactly `#050506 / #bf651d / #fba503` (7.16:1) = theme.md = page `:root`. No drift.
- **WORKFLOW.md shipped** (`research-paper/design/WORKFLOW.md`): the /goal deliverable — the pipeline S0→S9 as executed, every gate, the S4t treatment, and the S4t gate weakness (size-only; passes untreated plates; fix = palette census against PROVENANCE ink/paper, still open).
- **S10 remains blocked on his pick** — by design (picker is the artifact).

## Fleet landing — S10 green, pipeline complete (2026-08-24)
- **S10 run against lamplight (the built candidate) so his pick arrives pre-gated** — the picker still decides; a different pick re-runs S10 on that world. `design-gates.mjs` forced four fixes our own prover cannot see: `<main>` landmark, focusable + uniquely-labelled scrollable table regions (`tabindex=0 role=region`), `overflow-x:auto` on both table wrappers, `overflow-wrap:break-word` on h2 ("harbourmaster" clipped at 200% zoom on 390px). Final: axe 0 on all 4 devices, reflow/o200/clip200 all 0, INP p75 32ms.
- **Lighthouse perf 90→99: the 189KB WorkSans TTF was the whole gap.** Fonts subset to latin woff2 via `pyftsubset --flavor=woff2` (245KB→37KB across three faces, files committed in `fonts/`), preloads on the two above-the-fold faces, `width`/`height` on all four plates (CLS 0), `fetchpriority=high` on the first. Final: perf 99 / a11y 100 / LCP 2.0s / CLS 0 / TBT 0. Ephemeral http.server killed same turn; evidence + exact commands in `design/GATES.md`.
- **`design-pipeline.py status`: every stage S0–S10 PASSES — "NEXT: nothing".** WORKFLOW.md extended with the S10 section.
